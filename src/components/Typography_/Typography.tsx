import styles from './Typography.module.scss';

type TypographyProps = {
    variant?: 'display' | 'heading' | 'body';
};

export function Typography({ variant = 'body' }: TypographyProps) {
    return (<>
        
    </>);
}